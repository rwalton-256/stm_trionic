{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "command": "cmake -Bbuild"
        },
        {
            "label": "make",
            "type": "shell",
            "command": "cmake --build build -j 20",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "dependsOn":["cmake"]
        },
        {
            "label": "build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "dependsOn": ["make"]
        }
    ]
}
